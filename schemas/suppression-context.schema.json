{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/s977043/river-review/schemas/suppression-context.schema.json",
  "title": "Suppression Entry Context Schema",
  "description": "JSON Schema for the `context` payload of a Riverbed Memory entry whose `type` is `suppression`. The parent entry is validated by riverbed-entry.schema.json (which permits additional context properties); this schema constrains the suppression-specific shape so that auto-suppression and feedback typing remain consistent. See Issue #687 (Epic #650).",
  "type": "object",
  "additionalProperties": true,
  "required": ["scope", "active"],
  "$defs": {
    "feedbackType": {
      "type": "string",
      "enum": ["false_positive", "accepted_risk", "wont_fix", "not_relevant", "duplicate"],
      "description": "Why this suppression was added. Used by applySuppressions to decide whether auto-suppression is allowed for the finding's severity."
    },
    "scope": {
      "type": "string",
      "enum": ["global", "subsystem", "file"],
      "description": "Spatial scope at which the suppression should match future findings."
    },
    "severity": {
      "type": "string",
      "enum": ["info", "minor", "major", "critical"],
      "description": "Severity of the finding being suppressed at the time of suppression."
    },
    "fingerprintAlgo": {
      "type": "string",
      "enum": ["v1", "v2"],
      "description": "Which fingerprint algorithm produced `fingerprint`, and therefore which finding-side value applySuppressions matches it against. Both are the first 16 chars of the lowercase hex sha256 of a key string, and both build that key from the same base: `<ruleId>::<file>::<message>`, where ruleId defaults to \"unknown\", file defaults to \"\", and message is `(finding.message ?? finding.title ?? \"\")` lowercased, with runs of whitespace collapsed to one space, trimmed, then truncated to 60 chars. v1 (computeFingerprint in src/lib/finding-factory.mjs, the default) hashes that base as-is — no line — so it survives line drift but suppresses EVERY same-kind finding in the same file. v2 (computeFingerprintV2, #1797) hashes `<base>::L<line>`, where line is `finding.lineStart ?? finding.line` when that is an integer >= 1 and 0 otherwise (a finding with no line anchor), so it suppresses only the occurrence at that line; the trade-off is that it stops matching once the line shifts. Absent is read as v1; an unrecognized value is ignored and reported through applySuppressions' warn sink (fail-safe: it must not gate findings under v1 semantics)."
    }
  },
  "properties": {
    "scope": { "$ref": "#/$defs/scope" },
    "active": {
      "type": "boolean",
      "description": "Whether the suppression is currently in effect. Revocations are recorded as separate `resurface` entries; this flag must remain true for the suppression to be considered active by findActiveSuppressions."
    },
    "feedbackType": {
      "$ref": "#/$defs/feedbackType"
    },
    "fingerprint": {
      "type": "string",
      "minLength": 16,
      "maxLength": 16,
      "pattern": "^[0-9a-f]{16}$",
      "description": "16-hex sha256-derived fingerprint of the finding. Required for auto-suppression matching introduced in #687 PR-B. Which function produced it is declared by `fingerprintAlgo`: computeFingerprint (v1, the default) or computeFingerprintV2 (v2, line-anchored, #1797), both in src/lib/finding-factory.mjs."
    },
    "fingerprintAlgo": {
      "$ref": "#/$defs/fingerprintAlgo"
    },
    "severity": {
      "$ref": "#/$defs/severity"
    },
    "minSeverityToAutoSuppress": {
      "$ref": "#/$defs/severity",
      "description": "Optional override. When set, only findings whose severity is at or below this rank may be auto-suppressed by this entry. Default policy (#687 PR-B): findings of severity major/critical require feedbackType=accepted_risk."
    },
    "duplicateOfFingerprint": {
      "type": "string",
      "minLength": 16,
      "maxLength": 16,
      "pattern": "^[0-9a-f]{16}$",
      "description": "Set when feedbackType=duplicate. Points at the canonical fingerprint that this finding duplicates."
    },
    "findingId": {
      "type": ["string", "null"],
      "description": "Original transient finding ID at the time of suppression (regenerated each run; do not rely on for matching)."
    },
    "findingHash": {
      "type": ["string", "null"],
      "description": "Legacy hash from src/lib/suppression.mjs:hashFinding. Kept for backward compatibility; new entries should populate `fingerprint` instead."
    },
    "expiresAt": {
      "type": "string",
      "format": "date-time",
      "description": "Optional expiry; matches riverbed-entry.expiresAt semantics."
    },
    "sourcePR": {
      "type": "integer",
      "minimum": 1,
      "description": "PR number where the suppression was created."
    },
    "sourceCommentId": {
      "type": "integer",
      "minimum": 1,
      "description": "GitHub review comment ID that recorded the feedback."
    },
    "skillId": {
      "type": "string",
      "minLength": 1,
      "description": "Id of the skill that produced the suppressed finding (the finding's `ruleId`), recorded at issuance (#2202 Phase 0). Provenance only: not read by the expiry rule. Omitted when the skill is unknown."
    },
    "skillVersion": {
      "type": "string",
      "minLength": 1,
      "description": "The `version` of that skill's SKILL.md metadata at issuance (#2202 Phase 0). Provenance only: not read by the expiry rule. Omitted when the skill or its version is unknown."
    },
    "rulesDigest": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$",
      "description": "Lowercase hex sha256 of the project rules text (`.river/rules.md` plus `.river/rules.d/*.md`, as loaded by loadProjectRules and hashed by computeRulesDigest in src/lib/rules.mjs) at issuance (#2202 Phase 0). Provenance only: not read by the expiry rule. Omitted when the project has no rules."
    }
  }
}
