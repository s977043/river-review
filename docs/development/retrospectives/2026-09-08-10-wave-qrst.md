# Retrospective: 2026-09-08..10 Wave Q〜T Session

前回の振り返り PR #2141（`d0db1891`）以降、v1.108.0（`2f1e7b6a`）までを対象にします。`docs/development/improvement-flow.md` の手順に従い、品質ゲート（うまくいったことを含める / 影響を書く / 最低 1 件は自動ガード）を重ねています。

## Numerical summary

数値はすべて 2026-09-09T21:00Z 時点の実測です。測定コマンドを表の下に示します。

| Metric             | Value（2026-09-09T21:00Z 時点）                                  |
| ------------------ | ---------------------------------------------------------------- |
| Merged PRs         | 42（chore 12 / docs 10 / refactor 9 / feat 5 / fix 4 / test 2）  |
| Releases           | 8（v1.105.0 から v1.108.0）                                      |
| 新規 issue         | 1（#2193。**誤りのため訂正して close**）                         |
| クローズした issue | 2（#1415 #2144）                                                 |
| `src/cli.mjs`      | 2454 行 → 1805 行（`parseArgs` 804 行を 6 モジュールへ分割）     |
| Dependabot alert   | 5 → 0                                                            |
| 規律の還流         | 6 件（`worker-discipline-template.md` 5 / `/range-review` 1）    |
| 破壊的 git 操作    | 0（`git reflog` に `reset --hard` / `checkout --` / force なし） |
| 最終状態           | main `2f1e7b6a` / 自分の open PR 0 / open issue 17               |

```bash
git log --format='%s' c172c5e9..2f1e7b6a | grep -oE '\(#[0-9]+\)$' | tr -d '(#)' | sort -n | uniq | wc -l
git log --format='%s' c172c5e9..2f1e7b6a | grep -oE '^[a-z]+' | sort | uniq -c | sort -rn
git show c172c5e9:src/cli.mjs | wc -l && wc -l < src/cli.mjs
git reflog --date=iso | grep -cE "reset --hard|checkout -- |push --force"
```

## うまくいったこと

### 1. 着手前に安全網を測った

`parseArgs` の分割に入る前に変異注入で検出力を測り、**`--artifact` の値検証を外す変異がフルスイート 4718 件を全緑で通過する**ことを見つけました。先に pin を足して（PR #2167）から抽出に入っています。

影響は大きく、以降の全 6 段でその pin が実際に変異を捕まえました。着手後に気づいていれば、どの段で壊したかの切り分けに往復が発生します。

### 2. 敵対的レビューが CI 緑の PR を止めた

PR #2192（AC7 P4-1）で blocker 2 件。**CI も disposition もクリアでしたが、`await` 漏れと `when` 素通りを検出**しました。

根本原因は 1 つで、予約 primitive 用に独自の dispatch 経路を書いたことでした。`when` の評価と `onUnsatisfied` の 3 値と `await` を持つ共通経路を迂回しています。マージしていれば実行エンジンに欠陥が入りました。

### 3. 計画を実測が上書きした（3 例）

| 計画                              | 実測で分かったこと                                     | 変更                           |
| --------------------------------- | ------------------------------------------------------ | ------------------------------ |
| Step 1 で `--` ターミネータを抽出 | positional ヘルパーに依存する                          | Step 1 と 3 を入れ替え         |
| オプション連鎖をディスパッチ表へ  | 32 か所に `break` があり評価順が変わる                 | 順序を保って先頭から移す方式へ |
| P4-4 で CLI に judgment を注入    | `mode:'execute'` の呼び出しが**リポジトリ全体に 0 件** | P4-4 を取り下げ                |

いずれも計画どおり進めていれば壊していました。**着手前に依存や実態を測る手順が機能しています。**

### 4. 規律をその場で還流できるようにした

監査で見つけた権限の矛盾（PR #2188）を先に解消したことで、以降の還流が確認待ちにならなくなりました。実際、その後に 3 件の規律をその場で追記しています。

## 改善が必要なこと

| #   | 問題                                         | 発生箇所       | 影響                                                                                                        |
| --- | -------------------------------------------- | -------------- | ----------------------------------------------------------------------------------------------------------- |
| 1   | **存在しない不具合を起票した**               | Issue #2193    | probe が停止要因を 2 つ含んでいた。起票・訂正・close で往復。**公開した誤りなので読み手の判断も汚染しうる** |
| 2   | **push 済み commit を amend した**           | PR #2190       | force-push でしか解消できない分岐を作成。`reset --soft` で復旧したが禁止操作の一歩手前                      |
| 3   | **変異が適用されないまま「素通り」と読んだ** | wave Q で 3 回 | 守られていない箇所を守られていると誤認しかけた。`grep -c` で毎回気づいたが、気づかなければ穴を残した        |
| 4   | **メモリ逼迫で kill 10 回、flake 1 回**      | 全 wave        | 測定のやり直しが常態化。テスト 3 件の失敗を自分の変更由来と疑い切り分けに往復した                           |

## 改善アクション

**1 から 3 は本セッション内で還流済みです。** 新規 PR は作りません。

| #    | 分類 | 成果物                                            | PR    |
| ---- | ---- | ------------------------------------------------- | ----- |
| 1    | A    | probe には停止要因を 1 つだけ含める               | #2197 |
| 2    | A    | push 済み commit を amend しない                  | #2191 |
| 3    | B    | 変異を入れたら `grep -c` で適用件数を数える       | #2182 |
| 3    | B    | exit code だけを pin する表の限界                 | #2182 |
| 追加 | B    | 移設したブロックの import を機械照合する          | #2178 |
| 追加 | B    | 変異注入は呼び出し側の引数にも当てる              | #2163 |
| 追加 | C    | `/range-review` を Codex CLI で直列に回す退避手順 | #2164 |

4 は**リポジトリではなく実行環境の問題**です。ガード化の対象になりません。運用として「lint とテストを 1 コマンドにまとめず単独で回す」を carry-over に残しています。

### 自動ガードの追加（B）

本セッションでは規律の追記に加えて、**機械検査を 2 件**入れています。

- **heredoc サイズ検査**（PR #2186）。bash 5.3.15 が 512 バイト超の heredoc で deadlock する問題（#2144）の follow-up。#1951 がインスタンス単位で直しガードを残さなかったため 16 日後に再発した経緯があります。
- **列挙の実体を git 追跡対象に限定**（PR #2165）。未追跡ファイルが「実体」に混じり、CI は緑でローカルだけ赤くなる構造を解消しました。

## 派生した Issue

- **#2144 を close**（PR #2172）。bash 5.3.15 の heredoc deadlock。原因は #1951 が特定済みで、`pr-unstall.sh` がその 16 日後に追加されたため掃引の対象外だった。
- **#1415 を close**。node:test の IPC flake。実測で再発なしを確認。
- **#2193 を起票して訂正 close**。上記「改善が必要なこと」の 1 番。

## Carry-over observations

- **AC7 の execute 経路が未実装**。P4-4 を取り下げた理由。`review exec --entry` の steps は capability 未注入で全て `stopped` になる。別スライスとして設計が要る。
- **指示系監査の仮説 2 件が未検証**。`.claude/rules/review-core.md` の `globs: **/*` の要否と、gh 手動ガードが hook と重複している件。いずれも実測が要る。
- **`skills/agent-skills/as-*` の未追跡 5 ディレクトリ**が 2026-09-05 から残っている。旧版 CLI の書き込み跡と見られるが、意図して置かれた可能性もあり判断を保留している。

## 運用メモ

- **範囲レビューを 2 回スキップした**。wave S（2 commit / 13 行の文書変更）と wave T（実質 1 ファイルで敵対的レビュー済み）。`/range-review` は 3 視点で約 4,500 tokens を使うため、規模に見合わない場合は代替の実測で置き換えている。
- **release-please の `action_required` 停止に 3 回当たった**。いずれも `scripts/release-please-kick.sh` で解除。診断は head の作者と run の conclusion を測る既知手順。
- **gh アカウントの自動切り替えを 3 回検知**。うち 1 回は `gh pr create` が `must be a collaborator` で失敗してから気づいた。
