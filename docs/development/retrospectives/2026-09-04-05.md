# Retrospective: 2026-09-04..05 Session

前回の振り返り PR #2061（`c98f6995`）以降、v1.99.2（`c4453812`）までを対象にします。`docs/development/improvement-flow.md` の手順に従い、品質ゲート（うまくいったことを含める / 影響を書く / 最低 1 件は自動ガード）を重ねています。

## Numerical summary

数値はすべて 2026-09-05T06:00Z 時点の実測です。測定コマンドを各行の下に示します。

| Metric                       | Value（2026-09-05T06:00Z 時点）                                            |
| ---------------------------- | -------------------------------------------------------------------------- |
| Merged PRs                   | 4（feat 1 / fix 3）+ release 3                                             |
| Releases                     | 3（v1.99.0 / v1.99.1 / v1.99.2）                                           |
| 新規 issue                   | 6（#2065 #2067 #2071 #2074 #2075 #2076）                                   |
| クローズした issue           | 6                                                                          |
| 範囲 diff レビューの finding | blocker 0 / major 3 / minor 3（2 視点）                                    |
| 最終状態                     | main `c4453812` / 自分の open PR 2（#2077 #2078）/ 委託中 2（#2071 #2076） |

```bash
# Merged PRs（4 + release 3）
git log --oneline c98f6995..c4453812
# Releases（3）
gh release list --limit 5 --json tagName,publishedAt --jq '.[]|select(.publishedAt>="2026-09-04T05:00:00Z")'
# 新規 issue（6）
gh api --paginate 'repos/:owner/:repo/issues?state=all&since=2026-09-04T05:00:00Z&per_page=100' \
  --jq '.[]|select(.pull_request==null)|select(.created_at>="2026-09-04T05:00:00Z")|.number'
# クローズした issue（6）
gh api --paginate 'repos/:owner/:repo/issues?state=closed&since=2026-09-04T05:00:00Z&per_page=100' \
  --jq '[.[]|select(.pull_request==null)|select(.closed_at>="2026-09-04T05:00:00Z")]|length'
```

## うまくいったこと

### 範囲 diff レビューが per-PR レビュー 2 本と PlanGate を通過した事実誤りを出した

PR #2073 は `pages/reference/stable-interfaces.md` の免責節に「値を一度も読まない面が受理をやめても、その面での動作は変わらず」と自ら書いた。実測では `river doctor --base main` が doctor 自体を実行せず usage error（exit 1）になる。per-PR レビュー 2 本（網羅性 / 出荷可否）と PlanGate はこれを通過し、v1.99.2 として公開された。範囲 diff `95e20e82..c4453812` の公開契約レビューがこれを major として出し、#2075 として起票した。

per-PR レビューは「PR が主張したこと」を検証するが、「PR の主張が実態か」は検証しない。前回（2026-09-03..04）に続き 2 セッション連続で、範囲レビューがこの盲点を埋めた。

### 公開契約 PR のレビューが 3 巡目まで価値を出した

PR #2073 は 3 巡した。2 巡目で掃引漏れ（`skills import` / `runs diff`）、3 巡目で未知サブコマンドの覆い隠し（`river runs nosuch --base main` が `Unknown runs subcommand` でなく `--base is not supported` を返す）を出し、`isNamedSurface()` で修正された。1 巡で止めていれば両方を出荷していた。

### `Closes` 除去のマージ手順が機能した

PR #2073 の初回コミット `d5f919dc` の本文に `Closes #2065` が残っていた（push 済みのため amend は禁止）。squash message を `--subject` / `--body` で明示して除去し、マージ後に `gh api repos/:owner/:repo/issues/2065 --jq .state` が `open` を返すことを確認した。AC 2 は #2074 が引き取る。

### bot stall の脱出が 1 回で決まった

release PR #2072 は bot push により全チェックが `action_required` で停止していた。`scripts/release-please-kick.sh` → `scripts/wait-pr-ready.sh` → マージまで 6 分。runbook どおりで、追加の判断は不要だった。

## 改善が必要なこと

| #   | 問題                                                                                         | 発生箇所                        | 影響                                                                           |
| --- | -------------------------------------------------------------------------------------------- | ------------------------------- | ------------------------------------------------------------------------------ |
| 1   | 読み取り専用のレビュー agent が旧版 CLI を実行し、作業ツリーへ 141 件書き込んだ              | 範囲 diff レビューのワーカー    | 追跡ファイルへの影響なし。既存の untracked 5 件と mtime でしか弁別できなかった |
| 2   | PR が自ら追加した免責文言が事実誤りのまま、レビュー 2 本と PlanGate を通過した               | #2073 `stable-interfaces.md`    | **v1.99.2 として公開済み**。契約文書の誤りが利用者に見える                     |
| 3   | `gh pr checks` の `cancel` を required check の `fail` と読んだ                              | オーガナイザーのマージ判定      | 3 回発生。各回 `gh run view` で読み直すまで判断が止まった                      |
| 4   | zsh が未クォート変数をワード分割しない罠を踏んだ                                             | オーガナイザーの検証コマンド    | 誤測定 1 回。memory に既にあった事項の再発                                     |
| 5   | `readFileSync(path, 'utf8')` の index を byte offset として委託指示に書いた                  | オーガナイザーの #2068 委託指示 | ワーカーの実測（16480）と食い違い、指示側の数値が誤りだった                    |
| 6   | 委託指示の誤り 3 件（未 export の const を import させる / 偽の spec / allow-list 拡張指示） | オーガナイザーの #2062 委託指示 | ワーカー側で 3 件とも是正。CLAUDE.md「Fact-check what you author」の違反       |

### 1. 読み取り専用の指示は CLI の副作用を縛らない

レビュー agent は後方互換の検証のため `git archive 95e20e82` で旧版 CLI を展開して実行した。旧版ではまだ exit 0 だった `skills import` / `skills export` / `feedback add` / `suppression add` が成功し、展開先ではなく現在の作業ツリーへ書いた（`skills/agent-skills/as-*` 134 ディレクトリ、`.agents/`、`.river/feedback/2026-09.jsonl` と `.river/memory/index.json` に各 1 エントリ）。「ファイルを編集しない」はエディタ経由の編集しか縛らない。

`worker-discipline-template.md` へ 1 項目追記した（#2077）。

### 2. 免責文言はレビューで検証されにくい

免責は「この変更は破壊的でない」という PR 自身の主張であり、レビュアーはその主張の前提（挙動が変わらない）を実測せずに読む。#2073 では「その面での動作は変わらず」と書かれたが、実測は当該面の実行自体が行われない。

`docs/governance.md` の「Breaking change の扱い」に、免責を追加・拡張する PR は挙動を変更前後で実測し PR 本文に併記する、を追加した。

### 3. `gh pr checks` は同名 check の全 run を並べる

`gh pr checks` は同じ head に対する全 run を返す。test.yml は `concurrency: ci-${{ github.ref }}` / `cancel-in-progress: true` なので、再実行や再トリガーがあると `cancel` の古い run と `pass` の新しい run が同名で並ぶ。#2068 の head で `river-review` が 2 行（`pass@10:34:14` / `skipping@10:34:12`）並ぶことを実測した。

一方 `scripts/wait-pr-ready.sh` が使う check-runs API は既定 `filter=latest` で最新 run しか返さない（92ad9c15 で既定 22 件 / `filter=all` 23 件を実測）。誤読が起きたのは `gh pr checks` を直接読む経路だけである。

`/merge-check`（`.claude/commands/merge-check.md`）と `docs/governance.md` の `gh pr checks` に `group_by(.name) | map(max_by(.startedAt))` を入れ、同名 check の最新 run だけを見るようにした。#2068 で `river-review pass` の 1 行になることを確認した。

### 4〜6. 既存ガードの再発

4 は memory `feedback_zsh_no_word_split_file_loops`、6 は CLAUDE.md「Fact-check what you author」に既にある。新しい規律を足しても再発は防げないため、本書に再発として記録するにとどめる。5 は単位の問題で、`heuristic-detector-checklist.md` §1 へ「位置を報告するときは単位を明示する」を追加した。

## 改善アクション

| 分類                | 項目                                                    | 落とし先                                                 |
| ------------------- | ------------------------------------------------------- | -------------------------------------------------------- |
| A（委託規律）       | 旧版 CLI 実行の書き込み副作用（#1）                     | `worker-discipline-template.md`（#2077）                 |
| A（マージ前確認）   | 免責追加 PR は実測前後を PR 本文に併記する（#2）        | `docs/governance.md` Breaking change の扱い              |
| B（自動ガード）     | `gh pr checks` は同名 check の最新 run だけを見る（#3） | `.claude/commands/merge-check.md` / `docs/governance.md` |
| A（チェックリスト） | 位置の報告は単位を明示する（#5）                        | `heuristic-detector-checklist.md` §1                     |
| —                   | #4 / #6 は既存ガードの再発として記録のみ                | 本書                                                     |

## 派生した Issue

| Issue | 内容                                                                    |
| ----- | ----------------------------------------------------------------------- |
| #2071 | `resolveRefToCommit` と `findMergeBase` が別候補を採ると警告がズレる    |
| #2074 | 受理したオプションが消費されているかを `KNOWN_OPTION_TOKENS` 全件で検査 |
| #2075 | #2065 の公開ドキュメントが実態とズレる                                  |
| #2076 | `--base` 拒否メッセージに復旧手順が無い                                 |

## Carry-over observations

- `river skills --base main import` は `import/` ディレクトリが存在すると exit 0 でレビューを実走する。`takeTrailingPositional` が後置サブコマンドを解決するのは `review` と `evolve` のみで、`skills` には語彙チェックが無い。#2065 の穴が `skills` にだけ残っている。
- `.lychee.toml` の `include_fragments = "anchor-only"` はローカルの lychee 0.23.0 と非互換で、`npm run check:links*` がローカルで実行できない。config のコメントに既知として記載済みだが未解決。
- ワーカー 2 名が「ローカルに Node 22 が無く v26 で検証した」と報告した。前セッションで Node 22.22.2 による検証実績があるため、PATH 差の可能性が高い。Node 22 での検証は CI に委ねている。

## 運用メモ

- 範囲 diff レビューは 2 視点（ロジック敵対 / 公開契約）で実施した。前回は 3 視点だった。公開契約の視点が今回の major 3 件をすべて出しているので、公開契約に触る範囲では省略しないこと。
- 読み取り専用 agent の副作用は `git status --short` で追跡外ファイルを見ただけでは既存の untracked と区別できない。mtime での弁別が必要だった。
- 訂正（2026-09-05）: 本書の初版は既存の untracked `skills/agent-skills/as-*` 5 件を「`npm ci` 由来」と書いたが、Node 22.22.2 で `npm ci` を実行した基準線は 0 件で再現しない（#2091、`scripts/worker-bootstrap.sh` の自己実行）。5 件の由来は未特定。
